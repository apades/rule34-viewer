import React, { useEffect, useState } from 'react'
import { Image, StyleSheet, Text, View } from 'react-native'

export function comp_image({ navigation, route, origin_uri, preview_uri }) {}

const styles = StyleSheet.create({})
